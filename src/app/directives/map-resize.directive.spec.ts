import { MapResizeDirective } from './map-resize.directive';

describe('MapResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MapResizeDirective();
    expect(directive).toBeTruthy();
  });
});
