import { MarkerClickDirective } from './marker-click.directive';

describe('MarkerClickDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkerClickDirective();
    expect(directive).toBeTruthy();
  });
});
